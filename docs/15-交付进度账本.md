# GovernForge 交付进度账本

更新日期：2026-07-21

这是交付状态的唯一跟踪表。已完成项目不重复执行；只有状态为“我可继续”或“等你反馈”的项目才进入下一轮。

## 状态定义

- `已完成`：有 commit、命令输出或远程链接，且满足验收标准。
- `我可继续`：不依赖你的外部权限，我会直接实施。
- `等你反馈`：代码已准备好，需要你在真实环境执行并返回证据。
- `阻断`：前置权限、环境或责任人缺失，不能安全假设。

## 当前账本

| 项目 | 状态 | 已有证据 | 下一动作 | 负责人 |
|---|---|---|---|---|
| 远程仓库与 `main` 保护 | 已完成 | `scripts/verify-github-governance.ps1`；8/8 通过 | 不重复执行；维护规则即可 | Codex |
| AgentEvent v1、幂等、审批、超时 | 已完成（本地） | 后端测试 40/40；`test_agents_api.py`、`test_control_plane_completion.py` | 等真实 Codex/Claude 任务 | 你 |
| Codex/Claude Hook 真实验收 | 等你反馈 | 安装命令和用例见 `docs/13` | 各跑低风险、高风险、断网恢复 | 你 |
| 模型网关主备/预算 | 已完成（模拟上游） | 40/40；主备降级和预算门禁测试 | 接入两个真实供应商并对账 | 你 + FinOps |
| PR 证据完整性硬门禁 | 已完成（本地） | commit `3e180d1`；缺证据强制 block | 等真实 PR 正反例 | 你 |
| GitHub Check/Outbox | 已完成（代码） | Webhook、Check、死信和重放测试 | 等真实 GitHub API 401/429/5xx 演练 | 你 + Codex |
| 受控 Auto-merge | 已完成（本地）/ 等真实 PR | `test_auto_merge_is_opt_in_and_requires_complete_allow_evidence`；默认关闭和 allow 正例已覆盖 | 你演练冲突、失败 CI、待审批、SHA 变化反例 | 你 |
| CI/CD 与工作负载回滚 | 已完成（代码） | deploy workflow；`b364800` | 等 staging kubeconfig 实测 | 你 + SRE |
| 前端生产构建 | 已完成 | Next.js 17 路由构建通过 | 不重复执行，除非代码变更 | Codex |
| Compose/Kustomize/依赖扫描 | 已完成 | config、kustomize、pip-audit 通过 | 不重复执行，除非依赖或清单变更 | Codex |
| OIDC/SCIM 与密钥系统 | 阻断 | 代码和测试已有，目标系统未提供 | 提供 Issuer/Client/Secret 或密钥系统 | 你 + IAM |
| 数据库备份/PITR/RPO/RTO | 阻断 | 本地不声称生产恢复 | 提供目标 PostgreSQL/备份存储并演练 | 你 + DBA/SRE |
| 观测告警/SLO | 阻断 | Trace、指标、审计代码已有 | 提供 OTel/Prometheus/Grafana/告警接收端 | 你 + SRE |
| 知识助手 ACL/DLP/红队 | 阻断 | 功能和评测框架已有 | 提供企业知识与 ACL Owner，执行金标集 | 你 + Knowledge Owner |
| 最终签字 | 阻断 | 签字模板已准备 | Product/Engineering/Security/SRE/FinOps 签字 | 你 |
| Dependabot 依赖 PR | 等你处理 | #1–#6、#8–#10 已有 Codex 分支和本地验证；#7 TypeScript 7 与 Next 15 不兼容 | 你审查/合并已推送分支；#7 暂缓等待 Next/TS 生态兼容方案 | 你 + Codex |
| 交付账本同步 PR | 已完成 | PR #12 已合并到 `main`，merge commit `d4df48c` | 不重复执行；后续账本更新另开 PR | Codex |

## 已验证的远程 PR

PR：[GovernForge #11](https://github.com/wyxang0133/governforge/pull/11)

PR #11 已于 `2026-07-21` 合并，合并 commit 为 `84087f77a004b6b60133f40ef07e1ffb471ac16f`。合并后的 `main` CI 全部通过，包含 backend、frontend、PostgreSQL、browser-e2e、images、manifests，并成功发布 backend/frontend GHCR snapshot；main 上的 CODEOWNERS 已包含 `@wyxang0133 @WYXRE`。

PR：[GovernForge #12](https://github.com/wyxang0133/governforge/pull/12)

PR #12 已于 `2026-07-21` 合并到 `main`，合并 commit 为 `d4df48c4f1adf556c67e771399d51831fba7dd2d`。该 PR 只同步账本和依赖队列说明，不改变业务代码。

当前 staging Deploy 仍等待真实 `KUBE_CONFIG_B64`；没有集群凭据时不得把 Deploy skipped/failed 当作代码失败。

## 从现在到 Production Approved 的分轮计划

### 第 1 轮：合并后收口（当前）

- 已完成：PR #11 合并、main CI、browser-e2e、镜像构建和 GHCR snapshot。
- 已完成：PR #12 合并到账本，确认远程 `main` 为 `d4df48c`。
- Codex：继续维护账本和依赖队列 triage；不修改已稳定的业务代码。

### 第 2 轮：真实 Agent 与真实 PR

- 待你：按 `docs/13-交付前执行手册.md` 各跑一次 Codex 和 Claude Code Hook。
- 待你：创建一个普通 PR、一个敏感文件 PR、一个失败 CI PR，保存 PR/Check/Trace。
- Codex：根据真实响应修复 Hook、Webhook、Check、Outbox 或权限问题。
- 通过标准：allow/review/block 正反例可复现，旧 SHA 不覆盖新决策，高风险零误放行。

### 第 3 轮：真实模型网关与成本

- 待你：提供两个真实 OpenAI-compatible 上游和价格表，配置到 Secret/Environment。
- 待你：执行正常调用、429/5xx、超时、预算超限、密钥轮换和月度对账。
- Codex：修复真实供应商兼容性、重试/熔断、成本归因问题。
- 通过标准：主备降级可追踪，预算前置拦截，账单误差达到 FinOps 批准阈值。

### 第 4 轮：staging、备份与可观测

- 待你：提供 staging `KUBE_CONFIG_B64`、OIDC/密钥系统、PostgreSQL 备份和 OTel/Prometheus 接收端。
- 待你：执行 deploy、smoke、rollout 失败回滚、备份恢复、RPO/RTO 和告警演练。
- Codex：修复流水线、清单、健康检查、告警和回滚问题。
- 通过标准：staging 可部署、可观测、可回滚；备份恢复达到 Owner 批准的 RPO/RTO。

### 第 5 轮：安全、UAT 与生产签字

- 待你：组织 IAM、Security、SRE、FinOps、Product/Engineering Owner。
- 待你：完成 Secret/依赖/镜像扫描、OIDC/SCIM 撤销、知识助手 ACL/DLP/红队、成本/ROI 和 UAT。
- Codex：处理验收发现的代码问题并补充证据。
- 通过标准：所有 P0 门禁有证据、责任人和日期，`docs/03` 发布签字区完整。

### 第 6 轮：Production Approved

- 仅当第 1–5 轮全部通过，才允许 production Environment 审批和不可变 digest 发布。
- 任一门禁缺证据，状态保持 `NO-GO`，不得宣布上线。

## Dependabot PR 处理顺序

截图里的 `10 Open` 是 Dependabot 自动创建的维护 PR，不代表有 10 个业务需求。建议顺序：先处理 GitHub Actions 更新（#1–#5），再处理 postcss/eslint 等前端工具链（#6/#10），最后单独评估 Next 16、TypeScript 7、lucide-react 1.25 等 major 更新（#7–#9）。每个 PR 都要看 CI 和变更说明；major 更新不能批量无审查合并。

### 2026-07-21 triage 结果

| PR | 类型 | 当前观察 | 建议动作 |
|---|---|---|---|
| #1–#5 Actions 更新 | GitHub Actions | 已合并到 `codex/actions-dependabot-rollup`；YAML parser 通过；旧 action 版本引用清空 | 审查该 rollup 分支后合并，可替代逐个处理 #1–#5 |
| #6 `postcss` 8.5.19→8.5.20 | 前端依赖 patch | 已推送 `codex/postcss-8.5.20`；`npm ci`、`npm run build`、`npm audit --omit=dev --audit-level=high --registry=https://registry.npmjs.org/` 通过 | 可审查合并 |
| #10 `eslint` 8.57.1→10.7.0 | 前端工具链 major | 已推送 `codex/eslint-10.7.0`；迁移到 ESLint flat config 和 `eslint .`；`npm run lint`、`npm run build`、audit 通过 | 可审查合并；这是配置迁移，不只是版本号 |
| #7 `typescript` 5.9.3→7.0.2 | 前端工具链 major | 本地复现失败：Next 15.5.19 无法加载 `next.config.ts`；最小修补后 Next 又自动降级安装 `typescript@5.8.2` 并报 `Invalid Version: undefined` | 暂缓，不单独合并；等 Next/TypeScript 兼容链路明确后再做 |
| #8 `lucide-react` 0.460.0→1.25.0 | 前端依赖 major | 已推送 `codex/lucide-react-1.25.0`；`Github` 图标导出已迁移为通用 `GitBranch`；`npm run build`、audit 通过 | 可审查合并 |
| #9 `next` 15.5.19→16.2.10 | 前端框架 major | 已推送 `codex/next-16.2.10`；Next 自动更新 `tsconfig`；`npm run build`、`npx tsc --noEmit`、audit 通过 | 可审查合并；合并前建议让 GitHub CI 跑完整 browser-e2e |
